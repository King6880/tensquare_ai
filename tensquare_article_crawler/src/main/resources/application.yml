#配置请求端口
#配置服务名
#配置datasource
#配置jpa
server:
  port: 9014
spring:
  application:
  name: tensquare‐crawler-article #指定服务名
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/tensquare_article?characterEncoding=UTF8
    username: root
    password: root
  jpa:
    database: MySQL
    show-sql: true